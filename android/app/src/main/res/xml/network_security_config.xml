<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cho phép HTTP cleartext traffic tới backend dev (IP LAN) -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- IP LAN của backend (thay đổi nếu cần) -->
        <domain includeSubdomains="false">192.168.1.11</domain>
        <!-- Localhost cho emulator/test -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    
    <!-- VNPay sandbox (HTTPS) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">sandbox.vnpayment.vn</domain>
        <domain includeSubdomains="true">vnpayment.vn</domain>
    </domain-config>
</network-security-config>
